const URL_IMAGES = '/images'

const image = (imageName) => URL_IMAGES + '/' + id

module.exports = {
    boards,
    board,
    settings,
    items,
    item,
    assignments,
    members,
    member
}